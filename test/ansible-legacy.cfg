# custom ansible configuration for kitchen integration testing

[defaults]
interpreter_python = /usr/bin/python2.7
interpreter_python_fallback = [‘/usr/bin/python3’]
callback_whitelist = profile_roles, profile_tasks, timer
